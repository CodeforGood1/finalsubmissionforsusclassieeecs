# =============================================================
# SUSTAINABLE CLASSROOM LMS — ENVIRONMENT CONFIGURATION
# =============================================================
# Copy this file to .env and edit the two marked values.
# Everything else has safe working defaults.
#
#   cp .env.example .env
#
# =============================================================

# ------------------------------------------------------------------
# REQUIRED: Set these two values for your deployment
# ------------------------------------------------------------------

# Your server's LAN IP address (find with: ip addr show  or  hostname -I)
# Students connect to this IP from their browsers.
# Use 0.0.0.0 only for single-machine local testing.
DOCKER_HOST_ADDRESS=0.0.0.0

# Timezone — see https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Africa/Lagos

# ------------------------------------------------------------------
# DATABASE (defaults work — change only if you have a conflict)
# ------------------------------------------------------------------
DB_USER=lms_admin
DB_PASSWORD=SecureLocalDB2026
DB_NAME=sustainable_classroom

# ------------------------------------------------------------------
# ADMIN CREDENTIALS
# Change before going live. Restart backend after change.
# ------------------------------------------------------------------
ADMIN_EMAIL=admin@classroom.local
ADMIN_PASSWORD=Admin@2026

# ------------------------------------------------------------------
# SECURITY
# Generate a strong secret: openssl rand -hex 32
# ------------------------------------------------------------------
JWT_SECRET=ChangeThisToASecureRandomStringBeforeDeployment

# ------------------------------------------------------------------
# JITSI VIDEO CONFERENCING (internal passwords — change for security)
# ------------------------------------------------------------------
JITSI_PUBLIC_URL=https://localhost:8443
JICOFO_AUTH_PASSWORD=jicofo-offline-2026
JICOFO_COMPONENT_SECRET=jicofo-component-offline-2026
JVB_AUTH_PASSWORD=jvb-offline-2026

# ------------------------------------------------------------------
# EMAIL
# Default: MailHog (local email capture — no internet needed)
# View captured emails at: http://localhost:8025
# To use real SMTP, replace with your provider's settings below.
# ------------------------------------------------------------------
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_SECURE=false
EMAIL_FROM_NAME=Sustainable Classroom
EMAIL_FROM_ADDRESS=noreply@classroom.local

# Example: Gmail SMTP (requires internet)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-16-char-app-password
# EMAIL_FROM_ADDRESS=your-email@gmail.com
